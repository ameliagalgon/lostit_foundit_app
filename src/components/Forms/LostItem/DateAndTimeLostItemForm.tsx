import React from "react";
import {View, Text, StyleSheet} from "react-native";

class DateAndTimeLostItemForm extends React.PureComponent {
    render() {
        return(
            <View style={styles.container}>
                <Text/>
            </View>
        );
    }
}

const styles = StyleSheet.create({
    container: {
        flex: 1
    }
});

export default DateAndTimeLostItemForm;